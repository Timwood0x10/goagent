.DS_Store

.vscode
.trae/

.claude/
.codegraph/
.atomcode/
.codebase-memory/
.codescope/
.workbuddy/
.agnes/

plan/
bin/

# Local config overrides — copy from config.example.yml and edit.
config.yaml
.mcp.json

# Local LLM secrets (real api_key for the gate-3 regression scorer).
configs/ares.local.yaml

/**/.venv/

**/.env

*.excalidraw

*.log
**/logs/
!examples/**/*.log
**/logs/

# aresos-demo run artifacts: local evidence only, never tracked (README
# references representative traces from 16/17/18 demos instead).
examples/_fixtures/aresos-demo/*.log
examples/_fixtures/aresos-demo/logs/
examples/_fixtures/aresos-demo/task_output.md

examples/quant-trading/workflow/config.yml


examples/autonomous-evolution/config/config.y*ml
examples/11-knowledge-import/config.yaml

examples/finetune/config/server.y*ml

# Runtime artifacts from the autonomous evolution example
evolution_best.json
examples/autonomous-evolution/autonomous-evolution

Ares_docs/ares.yaml
/ares.yaml

# Example run configs hold real LLM keys once edited — never track them.
examples/*/ares.yaml

# Agent context dir (rounds/, generated by the coding agent)
.context/

# Compiled demo binaries (48MB+; build on demand via go run)
/26-runtime-scheduling-demo
/examples/26-runtime-scheduling-demo/26-runtime-scheduling-demo
/30-introspect-panel-demo
/examples/30-introspect-panel-demo/30-introspect-panel-demo

# Compiled ares serve/demo binary (60MB+; build on demand via go build)
/ares
/graph_demo

# Compiled example/demo run binaries emitted at repo root and examples/
# fixtures by `go build`/`go run` (e.g. 01-quickstart, mcp-registry,
# custom-store, eval). Buffed per-project; multi-MB each.
/01-quickstart
/02-tool-calling
/06-chaos-resilience
/07-human-in-loop
/09-full-app
/10-ga-full-evolution
/11-knowledge-import
/14-tool-discovery
/21-ai-assistant-integration
/22-evolution-blocks
/custom-store
/discovery
/eval
/external-tools
/mcp-registry
/examples/_fixtures/01-quickstart/01-quickstart

# Coverage profiles are local build artifacts (multi-MB, regenerated by
# `go test -coverprofile`); never track them.
cover.out
*.out
coverage.html
