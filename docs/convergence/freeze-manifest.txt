# 收敛冻结清单（Phase 0 产出，2026-09-07）
#
# 治理规则（ARCHITECTURE.md §三）：
#   R1  examples/ 顶层条目必须是 [examples] 的子集 —— 禁止新增 demo 目录。
#   R2  internal/ 顶层包必须是 [internal] 的子集 —— 禁止新增顶层包。
#   R3  internal/fabric/* 包已迁移到位（Phase 2b 落地）。
#   缺失（计划内删除，如 kernelscheduler→kernel、agentfabric→fabric/agent）只告警、不失败；新增一律失败。
#   巡查脚本：scripts/check_convergence_freeze.sh（Makefile ci-freeze + ci.yml 接入）。

[examples]
# Phase 4: all example code moved to examples/_fixtures/ (2026-09-07).
# Top-level examples/ now only holds _fixtures/, arena/ (yaml scenarios),
# and README.md. No new top-level example dirs allowed (R1 still enforced).
_fixtures
arena
README.md

[internal]
# M5 internalization (2026-09-09): type/interface domains for the former
# api/* exposure move here so sdk/ layers import internal directly and break
# the sdk<->api back-edge. api/* degrades to a pure forwarding layer. The
# implementation domains (llm, llmservice, knowledge, storage/*) keep their
# owned packages; these *api/*core/*exp/*client packages never re-implement.
agentipc
agentloop
agents
agentsyscall
apitools
ares_bootstrap
ares_callbacks
ares_config
ares_ctxutil
ares_events
ares_integration
ares_ratelimit
ares_security
ares_shutdown
aresrecovery
core
detector
discovery
embedding
errors
evidence
fabric
feedback
introspect
kernel
knowledge
knowledgeapi
llm
llmcore
llmexp
llmservice
llmsvcapi
logger
mcpclient
runtime
scoreutil
storage
tools
truncate
